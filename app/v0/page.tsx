'use client'

/**
 * PREVIZUALIZARE -- prototipul generat in v0, pastrat verbatim ca referinta
 * vizuala. NU e conectat la Supabase: `partners` si `bookings` sunt array-uri
 * goale in sursa originala, iar metricile sunt text fix. Ruta e izolata: tema
 * teal se aplica doar sub `.v0-scope`, restul aplicatiei nu e afectat.
 */

import { useEffect, useMemo, useState, type ReactNode } from 'react'
import { Activity, ArrowRight, CalendarDays, Check, ChevronRight, CircleDollarSign, Clock3, Home, LayoutDashboard, MapPin, Menu, Package, Search, Settings2, ShieldCheck, Star, Store, Users, X } from 'lucide-react'

const mascot = 'https://hebbkx1anhila5yf.public.blob.vercel-storage.com/IMG_0059-DHCUOGU6GynoJZaAB52VVvtc0sXOX4.png'
const categories = [
  { title: 'Sporturi', meta: 'Terenuri & activități', image: 'https://images.unsplash.com/photo-1622163642998-1ea32b0bbc67?auto=format&fit=crop&w=900&q=80' },
  { title: 'Frizerii', meta: 'Servicii locale', image: 'https://images.unsplash.com/photo-1599351431202-1e0f0137899a?auto=format&fit=crop&w=900&q=80' },
  { title: 'Saloane', meta: 'Relaxare & îngrijire', image: 'https://images.unsplash.com/photo-1560066984-138dadb4c035?auto=format&fit=crop&w=900&q=80' },
  { title: 'Wellness', meta: 'Masaj, stretching & recuperare', image: 'https://images.unsplash.com/photo-1544161515-4ab6ce6db874?auto=format&fit=crop&w=900&q=80' },
  { title: 'Dans & mișcare', meta: 'Clase & antrenamente', image: 'https://images.unsplash.com/photo-1508807526345-15e9b5f4eaff?auto=format&fit=crop&w=900&q=80' },
  { title: 'Transport', meta: 'În curând', image: 'https://images.unsplash.com/photo-1558618666-fcd25c85cd64?auto=format&fit=crop&w=900&q=80' },
  { title: 'Auto', meta: 'Servicii auto', image: 'https://images.unsplash.com/photo-1486006920555-c77dcf18193c?auto=format&fit=crop&w=900&q=80' },
]
const partners: { name: string; type: string; rating: string; time: string; image: string }[] = []
const bookings: { person: string; service: string; time: string; status: string }[] = []

function Mascot({ className = 'size-12' }: { className?: string }) {
  return <span className={`relative block overflow-hidden rounded-full bg-[#56a9a5] ${className}`}><img src={mascot} alt="Plani, mascota PLANNO" className="absolute left-[-74%] top-[-186%] h-auto w-[250%] max-w-none" /></span>
}
function Logo() { return <a href="#acasa" className="text-[1.05rem] font-medium lowercase tracking-[-0.08em] transition-opacity hover:opacity-70 active:scale-[0.98]">planno</a> }
function Status({ children, tone = 'teal' }: { children: ReactNode; tone?: 'teal' | 'orange' | 'blue' }) { return <span className={`inline-flex rounded-full px-2.5 py-1 text-[11px] font-semibold ${tone === 'orange' ? 'bg-primary/15 text-primary' : tone === 'blue' ? 'bg-[#b9d9e8]/15 text-[#b9d9e8]' : 'bg-[#56a9a5]/15 text-[#75c8c1]'}`}>{children}</span> }
function Card({ children, className = '' }: { children: ReactNode; className?: string }) { return <div className={`rounded-2xl border border-border bg-card transition-all duration-300 hover:-translate-y-0.5 hover:border-[#56a9a5]/40 hover:shadow-lg hover:shadow-[#56a9a5]/5 ${className}`}>{children}</div> }
function TopBar({ title, subtitle, onMenu }: { title: string; subtitle: string; onMenu: () => void }) { return <header className="flex items-center justify-between border-b border-border px-5 py-4 sm:px-8"><button type="button" onClick={onMenu} className="rounded-lg p-2 text-muted-foreground lg:hidden" aria-label="Deschide meniul"><Menu className="size-5" /></button><div className="flex-1 lg:pl-0"><h1 className="text-xl font-semibold tracking-tight">{title}</h1><p className="mt-1 text-xs text-muted-foreground">{subtitle}</p></div><div className="flex items-center gap-3"><button type="button" className="hidden rounded-lg border border-border px-3 py-2 text-xs text-muted-foreground sm:block">Ajutor</button><Mascot className="size-9" /></div></header> }
function Sidebar({ area, onArea, mobileOpen }: { area: string; onArea: (v: string) => void; mobileOpen: boolean }) { const items = area === 'client' ? [{ id: 'home', label: 'Acasă', icon: Home }, { id: 'search', label: 'Caută', icon: Search }, { id: 'bookings', label: 'Programări', icon: CalendarDays }, { id: 'profile', label: 'Profil', icon: Users }] : area === 'merchant' ? [{ id: 'overview', label: 'Overview', icon: LayoutDashboard }, { id: 'calendar', label: 'Calendar', icon: CalendarDays }, { id: 'services', label: 'Servicii', icon: Package }, { id: 'customers', label: 'Clienți', icon: Users }, { id: 'payments', label: 'Încasări', icon: CircleDollarSign }] : [{ id: 'overview', label: 'Overview', icon: LayoutDashboard }, { id: 'merchants', label: 'Comercianți', icon: Store }, { id: 'customers', label: 'Clienți', icon: Users }, { id: 'bookings', label: 'Rezervări', icon: CalendarDays }, { id: 'settings', label: 'Setări', icon: Settings2 }]; return <aside className={`${mobileOpen ? 'fixed inset-y-0 left-0 z-50 flex' : 'hidden'} w-64 shrink-0 flex-col border-r border-border bg-sidebar p-5 lg:flex`}><div className="mb-10 flex items-center justify-between"><Logo /><button type="button" onClick={() => onArea(area)} className="lg:hidden" aria-label="Închide meniul"><X className="size-5" /></button></div><p className="mb-3 px-3 text-[10px] font-bold uppercase tracking-[0.18em] text-muted-foreground">Spațiul tău</p><nav className="flex flex-col gap-1">{items.map(({ id, label, icon: Icon }) => <button type="button" key={id} onClick={() => onArea(id)} className={`flex items-center gap-3 rounded-xl px-3 py-3 text-left text-sm transition-colors ${area === id ? 'bg-primary text-primary-foreground transition-all duration-200 hover:brightness-110 active:scale-[0.98]' : 'text-muted-foreground hover:bg-white/5 hover:text-foreground'}`}><Icon className="size-4" />{label}</button>)}</nav><div className="mt-auto rounded-2xl border border-[#56a9a5]/20 bg-[#56a9a5]/10 p-4"><Mascot className="size-10 animate-[pulse_3s_ease-in-out_infinite]" /><p className="mt-3 text-sm font-semibold">Salut, sunt Plani</p><p className="mt-1 text-xs leading-5 text-muted-foreground">Ține-ți programările sub control.</p></div></aside> }
function DashboardShell({ area, setArea, children, title, subtitle }: { area: string; setArea: (v: string) => void; children: ReactNode; title: string; subtitle: string }) { const [mobileOpen, setMobileOpen] = useState(false); return <div className="flex min-h-screen bg-background"><Sidebar area={area} onArea={(v) => { setArea(v); setMobileOpen(false) }} mobileOpen={mobileOpen} /><div className="min-w-0 flex-1"><TopBar title={title} subtitle={subtitle} onMenu={() => setMobileOpen(true)} /><main className="mx-auto max-w-7xl p-5 sm:p-8">{children}</main></div></div> }
function Metric({ label, value, change, icon: Icon }: { label: string; value: string; change: string; icon: typeof Activity }) { return <Card className="p-5"><div className="flex items-start justify-between"><div><p className="text-xs text-muted-foreground">{label}</p><p className="mt-3 text-2xl font-semibold tracking-tight">{value}</p><p className="mt-2 text-xs text-[#75c8c1]">{change}</p></div><span className="rounded-xl bg-primary/10 p-2.5 text-primary"><Icon className="size-4" /></span></div></Card> }
function MerchantDashboard() { const [section, setSection] = useState('overview'); return <DashboardShell area={section} setArea={setSection} title="Spațiul comerciantului" subtitle="Gestionează-ți activitatea"><div className="mb-8 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-sm text-muted-foreground">Aici vezi ce se întâmplă azi.</p><h2 className="mt-2 text-3xl font-semibold tracking-tight">Agenda ta, la zi.</h2></div><button type="button" className="flex items-center justify-center gap-2 rounded-xl bg-primary px-4 py-3 text-sm font-semibold text-primary-foreground"><Package className="size-4" /> Adaugă serviciu</button></div>{section === 'overview' && <><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Metric label="Rezervări azi" value="24" change="+18% față de ieri" icon={CalendarDays} /><Metric label="Venit estimat" value="2.840 lei" change="+12% săptămâna asta" icon={CircleDollarSign} /><Metric label="Clienți noi" value="18" change="+6 luna aceasta" icon={Users} /><Metric label="Rating" value="4.9" change="+0.2 în ultimele 30 zile" icon={Star} /></div><div className="mt-6 grid gap-6 xl:grid-cols-[1.4fr_1fr]"><Card className="p-5"><div className="mb-5 flex items-center justify-between"><div><h3 className="font-semibold">Programările de azi</h3><p className="mt-1 text-xs text-muted-foreground">Marți, 24 septembrie</p></div><button type="button" onClick={() => setSection('calendar')} className="text-xs font-semibold text-primary">Vezi calendarul <ChevronRight className="inline size-3" /></button></div><div className="divide-y divide-border">{bookings.map((booking) => <div className="flex items-center gap-3 py-4" key={booking.person}><div className="flex size-10 items-center justify-center rounded-full bg-[#b9d9e8]/20 text-sm font-semibold text-[#b9d9e8]">{booking.person.split(' ').map((x) => x[0]).join('')}</div><div className="min-w-0 flex-1"><p className="truncate text-sm font-medium">{booking.person}</p><p className="mt-1 text-xs text-muted-foreground">{booking.service} · {booking.time}</p></div><Status tone={booking.status === 'În așteptare' ? 'orange' : 'teal'}>{booking.status}</Status></div>)}</div></Card><Card className="p-5"><h3 className="font-semibold">Performanța săptămânii</h3><p className="mt-1 text-xs text-muted-foreground">Rezervări confirmate</p><div className="mt-8 flex h-40 items-end gap-2">{['55%', '72%', '48%', '82%', '68%', '94%', '42%'].map((height, index) => <div className="flex flex-1 flex-col items-center gap-2" key={height + index}><div className={`w-full rounded-t-lg ${index === 5 ? 'bg-primary' : 'bg-[#56a9a5]/45'}`} style={{ height }} /><span className="text-[10px] text-muted-foreground">{['L', 'Ma', 'Mi', 'J', 'V', 'S', 'D'][index]}</span></div>)}</div></Card></div></>}{section === 'calendar' && <TablePanel title="Calendar și rezervări" action="Rezervare nouă" rows={bookings} />}{section === 'services' && <TablePanel title="Serviciile tale" action="Serviciu nou" rows={[{ person: 'Tuns + barbă', service: '60 min · 80 lei', time: 'Activ', status: 'Popular' }, { person: 'Tuns clasic', service: '40 min · 55 lei', time: 'Activ', status: 'Disponibil' }, { person: 'Styling', service: '45 min · 65 lei', time: 'Activ', status: 'Disponibil' }]} />}{section === 'customers' && <TablePanel title="Clienții tăi" action="Exportă listă" rows={bookings.map((x) => ({ ...x, person: x.person, service: '12 vizite · client fidel' }))} />}{section === 'payments' && <div className="grid gap-4 sm:grid-cols-3"><Metric label="Încasări luna aceasta" value="18.420 lei" change="+14%" icon={CircleDollarSign} /><Metric label="În așteptare" value="1.240 lei" change="Se procesează" icon={Clock3} /><Metric label="Comision PLANNO" value="920 lei" change="Luna curentă" icon={Activity} /></div>}</DashboardShell> }
function TablePanel({ title, action, rows }: { title: string; action: string; rows: { person: string; service: string; time: string; status: string }[] }) { return <Card className="overflow-hidden"><div className="flex items-center justify-between border-b border-border p-5"><div><h2 className="font-semibold">{title}</h2><p className="mt-1 text-xs text-muted-foreground">Actualizat acum câteva minute</p></div><button type="button" className="rounded-xl bg-primary px-3 py-2 text-xs font-semibold text-primary-foreground">{action}</button></div><div className="divide-y divide-border">{rows.map((row) => <div className="flex flex-col gap-3 p-5 sm:flex-row sm:items-center" key={row.person}><div className="flex-1"><p className="text-sm font-medium">{row.person}</p><p className="mt-1 text-xs text-muted-foreground">{row.service}</p></div><span className="text-xs text-muted-foreground">{row.time}</span><Status tone={row.status === 'În așteptare' ? 'orange' : 'teal'}>{row.status}</Status><button type="button" className="text-xs font-semibold text-primary">Detalii</button></div>)}</div></Card> }
function AdminDashboard() { const [section, setSection] = useState('overview'); const merchants: { name: string; type: string; status: string; bookings: string }[] = []; return <DashboardShell area={section} setArea={setSection} title="Admin PLANNO" subtitle="Control center · actualizat acum 2 minute"><div className="mb-8"><p className="text-sm text-muted-foreground">Bun venit în centrul de control.</p><h2 className="mt-2 text-3xl font-semibold tracking-tight">Platforma merge bine.</h2></div>{section === 'overview' && <><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Metric label="Utilizatori activi" value="12.480" change="+8.4% luna aceasta" icon={Users} /><Metric label="Comercianți" value="248" change="12 cereri noi" icon={Store} /><Metric label="Rezervări" value="38.920" change="+16.2% luna aceasta" icon={CalendarDays} /><Metric label="Volum platformă" value="284k lei" change="+11.8% luna aceasta" icon={CircleDollarSign} /></div><div className="mt-6 grid gap-6 lg:grid-cols-[1.2fr_0.8fr]"><Card className="p-5"><div className="mb-5 flex items-center justify-between"><div><h3 className="font-semibold">Comercianți recenți</h3><p className="mt-1 text-xs text-muted-foreground">Verifică și aprobă cererile noi.</p></div><button type="button" onClick={() => setSection('merchants')} className="text-xs font-semibold text-primary">Vezi toți <ChevronRight className="inline size-3" /></button></div><div className="divide-y divide-border">{merchants.map((merchant) => <div key={merchant.name} className="flex items-center gap-3 py-4"><div className="flex size-10 items-center justify-center rounded-xl bg-[#56a9a5]/15 text-[#75c8c1]"><Store className="size-4" /></div><div className="flex-1"><p className="text-sm font-medium">{merchant.name}</p><p className="mt-1 text-xs text-muted-foreground">{merchant.type} · {merchant.bookings}</p></div><Status tone={merchant.status === 'În verificare' ? 'orange' : 'teal'}>{merchant.status}</Status></div>)}</div></Card><Card className="flex flex-col items-center justify-center p-7 text-center"><Mascot className="size-28" /><p className="mt-5 text-lg font-semibold">Plani monitorizează tot.</p><p className="mt-2 max-w-xs text-sm leading-6 text-muted-foreground">Ai 12 cereri de comercianți care așteaptă verificarea ta.</p><button type="button" onClick={() => setSection('merchants')} className="mt-5 rounded-xl bg-primary px-4 py-2.5 text-xs font-semibold text-primary-foreground">Verifică cererile</button></Card></div></>}{section === 'merchants' && <TablePanel title="Comercianți" action="Adaugă comerciant" rows={merchants.map((x) => ({ person: x.name, service: x.type, time: x.bookings, status: x.status }))} />}{section === 'customers' && <TablePanel title="Clienți" action="Exportă clienți" rows={bookings.map((x, i) => ({ person: x.person, service: `${120 + i * 42} lei cheltuiți · client activ`, time: `Ultima vizită: ${x.time}`, status: 'Activ' }))} />}{section === 'bookings' && <TablePanel title="Toate rezervările" action="Filtrează" rows={bookings.concat(bookings).map((x, i) => ({ ...x, person: `${x.person} ${i + 1}` }))} />}{section === 'settings' && <Card className="p-6"><h2 className="font-semibold">Setări platformă</h2><p className="mt-2 text-sm text-muted-foreground">Permisiuni, categorii, notificări și configurarea comisioanelor.</p></Card>}</DashboardShell> }
function PlaniWidget({ open, onToggle }: { open: boolean; onToggle: () => void }) { return <><button type="button" onClick={onToggle} aria-label={open ? 'Închide Plani' : 'Deschide Plani'} className="fixed bottom-6 right-6 z-40 flex items-center gap-3 rounded-full border border-[#56a9a5]/40 bg-card px-3 py-3 shadow-2xl shadow-[#56a9a5]/20 transition-all duration-300 hover:-translate-y-1 hover:border-[#75c8c1] active:scale-[0.98] animate-[pulse_4s_ease-in-out_infinite]"><Mascot className="size-11" /><span className="hidden pr-2 text-sm font-semibold sm:block">Descoperă cu Plani</span></button><div className={`fixed inset-x-4 bottom-24 z-30 mx-auto max-w-md origin-bottom rounded-3xl border border-[#56a9a5]/30 bg-card p-5 shadow-2xl shadow-black/40 transition-all duration-500 ${open ? 'translate-y-0 opacity-100' : 'pointer-events-none translate-y-8 opacity-0'}`}><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[0.16em] text-[#75c8c1]">Plani recomandă</p><h2 className="mt-2 text-lg font-semibold">Găsește ceva bun pentru azi</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">Spune-ne ce cauți și îți arătăm locuri potrivite în orașul, comuna sau localitatea ta.</p></div><Mascot className="size-14 shrink-0" /></div><button type="button" onClick={onToggle} className="mt-5 w-full rounded-xl bg-primary px-4 py-3 text-sm font-semibold text-primary-foreground transition-all hover:brightness-110 active:scale-[0.98]">Începe căutarea</button></div></> }
function ClientHome({ go }: { go: (v: string) => void }) { const [city, setCity] = useState('București'); const [query, setQuery] = useState(''); const [selected, setSelected] = useState(''); const [promptIndex, setPromptIndex] = useState(0); const [planiOpen, setPlaniOpen] = useState(false); const prompts = ['un salon în apropiere', 'un teren de sport pentru azi', 'un serviciu auto de încredere']; useEffect(() => { const timer = window.setInterval(() => setPromptIndex((index) => (index + 1) % prompts.length), 2600); return () => window.clearInterval(timer) }, [prompts.length]); const filtered = useMemo(() => selected ? categories.filter((x) => x.title === selected) : categories, [selected]); return <><div className="min-h-screen bg-background pb-12"><header className="mx-auto flex max-w-7xl items-center justify-between px-5 py-5 sm:px-8"><Logo /><nav className="hidden items-center gap-7 text-sm text-muted-foreground md:flex"><a href="#exploreaza" className="hover:text-foreground">Explorează</a><a href="#parteneri" className="hover:text-foreground">Parteneri</a><button type="button" onClick={() => go('merchant')} className="hover:text-primary">Pentru business</button></nav><div className="flex items-center gap-3"><button type="button" onClick={() => go('client')} className="hidden text-sm text-muted-foreground sm:block">Contul meu</button><button type="button" onClick={() => go('merchant')} className="rounded-xl bg-primary px-4 py-2.5 text-sm font-semibold text-primary-foreground">Intră în spațiu</button></div></header><section className="mx-auto max-w-7xl px-5 pb-20 pt-14 sm:px-8 sm:pt-24"><div className="max-w-3xl"><Status tone="teal">Experiențe & rezervări simple</Status><h1 className="mt-6 max-w-2xl text-balance text-5xl font-semibold tracking-[-0.07em] sm:text-7xl">Cât ai zice <span className="text-primary">Planno.</span></h1><p className="mt-6 max-w-xl text-base leading-7 text-muted-foreground sm:text-lg">Locul tău pe sport, îngrijire, mobilitate și servicii, la frizerie, salon sau transport — rezervat în câteva secunde, fără apeluri.</p><div className="mt-9 flex max-w-2xl flex-col gap-2 rounded-2xl border border-border bg-card p-2 shadow-2xl sm:flex-row sm:items-center"><Search className="mx-3 size-5 text-muted-foreground" /><input value={query} onChange={(e) => setQuery(e.target.value)} className="min-w-0 flex-1 bg-transparent px-2 py-3 text-sm outline-none placeholder:text-muted-foreground" placeholder={`Caută ${prompts[promptIndex]}` } aria-label="Caută servicii" /><select value={city} onChange={(e) => setCity(e.target.value)} className="rounded-xl bg-secondary px-3 py-3 text-sm outline-none"><option>București</option><option>Roman</option><option>Toate orașele</option></select><button type="button" onClick={() => document.querySelector('#parteneri')?.scrollIntoView({ behavior: 'smooth' })} className="rounded-xl bg-primary px-5 py-3 text-sm font-semibold text-primary-foreground">Caută</button></div><p className="mt-3 text-xs text-muted-foreground">Rezervări rapide în {city}, fără stres.</p></div></section><section id="exploreaza" className="mx-auto max-w-7xl px-5 sm:px-8"><div className="mb-6 flex items-end justify-between"><div><p className="mb-2 text-xs font-semibold uppercase tracking-[0.18em] text-primary">Descoperă</p><h2 className="text-2xl font-semibold">Categorii populare</h2></div>{selected && <button type="button" onClick={() => setSelected('')} className="text-xs text-muted-foreground underline">Vezi toate</button>}</div><div className="grid grid-cols-2 gap-3 sm:grid-cols-3 lg:grid-cols-5">{filtered.map((category) => <button type="button" onClick={() => setSelected(category.title)} key={category.title} className="group relative h-44 overflow-hidden rounded-2xl border border-border text-left"><img src={category.image} alt={category.title} className="absolute inset-0 size-full object-cover transition-transform duration-500 group-hover:scale-105" /><div className="absolute inset-0 bg-gradient-to-t from-black/80 to-transparent" /><div className="absolute inset-x-4 bottom-4"><span className="text-[10px] font-semibold uppercase tracking-wider text-[#b9d9e8]">{category.meta}</span><h3 className="mt-1 text-xl font-semibold text-white">{category.title}</h3></div></button>)}</div></section><section id="parteneri" className="mx-auto mt-20 max-w-7xl px-5 sm:px-8"><div className="mb-6"><p className="mb-2 text-xs font-semibold uppercase tracking-[0.18em] text-primary">Aproape de tine</p><h2 className="text-2xl font-semibold">Alege și rezervă direct</h2></div><div className="grid gap-4 md:grid-cols-3">{partners.map((partner) => <Card key={partner.name} className="overflow-hidden"><div className="relative h-40"><img src={partner.image} alt={partner.name} className="size-full object-cover" /><span className="absolute left-3 top-3 rounded-full bg-black/60 px-2 py-1 text-xs text-white"><Star className="mr-1 inline size-3 fill-primary text-primary" />{partner.rating}</span></div><div className="p-4"><h3 className="font-semibold">{partner.name}</h3><p className="mt-1 text-xs text-muted-foreground">{partner.type}</p><div className="mt-4 flex items-center justify-between border-t border-border pt-3"><span className="text-xs text-muted-foreground"><Clock3 className="mr-1 inline size-3" />{partner.time}</span><button type="button" onClick={() => go('client')} className="rounded-lg bg-primary px-3 py-2 text-xs font-semibold text-primary-foreground">Rezervă</button></div></div></Card>)}</div></section><section className="mx-5 mt-20 rounded-3xl border border-[#56a9a5]/25 bg-[#56a9a5]/10 p-7 sm:mx-8 sm:p-10"><div className="mx-auto flex max-w-7xl flex-col items-start justify-between gap-6 md:flex-row md:items-center"><div><span className="text-[10px] font-bold uppercase tracking-wider text-[#75c8c1]">Pentru business</span><h2 className="mt-3 text-2xl font-semibold">Ai un loc bun? Fă-l ușor de rezervat.</h2><p className="mt-2 max-w-xl text-sm leading-6 text-muted-foreground">Îți umpli agenda, gestionezi clienții și vezi încasările într-un singur loc.</p></div><button type="button" onClick={() => go('merchant')} className="rounded-xl bg-primary px-5 py-3 text-sm font-semibold text-primary-foreground">Vezi spațiul comerciantului <ArrowRight className="ml-1 inline size-4" /></button></div></section><footer className="mx-auto mt-20 flex max-w-7xl justify-between border-t border-border px-5 py-8 text-xs text-muted-foreground sm:px-8"><span>© 2025 PLANNO</span><button type="button" onClick={() => go('admin')} className="hover:text-foreground">Admin demo</button></footer></div><PlaniWidget open={planiOpen} onToggle={() => setPlaniOpen((open) => !open)} /></> }
function ClientDashboard() { const [section, setSection] = useState('home'); return <DashboardShell area={section} setArea={setSection} title="Contul tău" subtitle="Programări simple, într-un singur loc"><div className="mb-8"><p className="text-sm text-muted-foreground">Ai totul într-un singur loc.</p><h2 className="mt-2 text-3xl font-semibold tracking-tight">Programările tale.</h2></div>{section === 'home' && <><Card className="flex flex-col items-start gap-5 overflow-hidden bg-[#56a9a5]/10 p-6 sm:flex-row sm:items-center"><Mascot className="size-20 shrink-0" /><div className="flex-1"><p className="text-xs font-semibold uppercase tracking-wider text-[#75c8c1]">Următoarea programare</p><h3 className="mt-2 text-xl font-semibold">Tuns + barbă · The Barber Club</h3><p className="mt-2 text-sm text-muted-foreground"><CalendarDays className="mr-1 inline size-4" /> Mâine, 10:00 · Roman</p></div><Status>Confirmată</Status></Card><h3 className="mb-4 mt-8 font-semibold">Istoricul tău</h3><TablePanel title="Programările mele" action="Rezervare nouă" rows={bookings.map((x) => ({ ...x, person: x.service, service: x.person }))} /></>}{section === 'bookings' && <TablePanel title="Programările mele" action="Rezervare nouă" rows={bookings.map((x) => ({ ...x, person: x.service, service: x.person }))} />}{section === 'profile' && <Card className="max-w-xl p-6"><div className="flex items-center gap-4"><div className="flex size-14 items-center justify-center rounded-full bg-[#b9d9e8]/20 text-lg font-semibold text-[#b9d9e8]">MP</div><div><h2 className="font-semibold">Maria Popescu</h2><p className="text-sm text-muted-foreground">maria@example.com</p></div></div><div className="mt-6 grid gap-3"><label className="text-xs text-muted-foreground">Nume complet<input className="mt-2 w-full rounded-xl border border-border bg-secondary p-3 text-sm outline-none" defaultValue="Maria Popescu" /></label><label className="text-xs text-muted-foreground">Oraș<input className="mt-2 w-full rounded-xl border border-border bg-secondary p-3 text-sm outline-none" defaultValue="București" /></label><button type="button" className="mt-2 rounded-xl bg-primary px-4 py-3 text-sm font-semibold text-primary-foreground">Salvează modificările</button></div></Card>}</DashboardShell> }
const V0_THEME = `
.v0-scope {
  /* Paleta din globals.css-ul prototipului v0 (teal-tinted dark). */
  --color-background: oklch(0.145 0.035 190);
  --color-foreground: oklch(0.985 0.01 190);
  --color-card: oklch(0.19 0.045 188);
  --color-card-foreground: oklch(0.985 0.01 190);
  --color-muted: oklch(0.25 0.05 190);
  --color-muted-foreground: oklch(0.72 0.035 190);
  --color-border: oklch(1 0.03 190 / 12%);
  --color-accent: oklch(0.63 0.12 187);
  --v0-primary: oklch(0.72 0.16 48);
  --v0-primary-foreground: oklch(0.15 0.035 190);
  --v0-secondary: oklch(0.25 0.05 190);
  --v0-sidebar: oklch(0.17 0.007 285);
  background-color: var(--color-background);
  color: var(--color-foreground);
}
/* primary/secondary/sidebar nu sunt mapate in @theme-ul proiectului,
   deci utilitarele astea nu exista -- le definim doar in interiorul rutei. */
.v0-scope .bg-primary { background-color: var(--v0-primary); }
.v0-scope .bg-primary\\/10 { background-color: color-mix(in oklab, var(--v0-primary) 10%, transparent); }
.v0-scope .bg-primary\\/15 { background-color: color-mix(in oklab, var(--v0-primary) 15%, transparent); }
.v0-scope .text-primary { color: var(--v0-primary); }
.v0-scope .text-primary-foreground { color: var(--v0-primary-foreground); }
.v0-scope .fill-primary { fill: var(--v0-primary); }
.v0-scope .bg-secondary { background-color: var(--v0-secondary); }
.v0-scope .bg-sidebar { background-color: var(--v0-sidebar); }
`

export default function V0PreviewPage() {
  const [view, setView] = useState('home')
  return (
    <div className="v0-scope min-h-screen">
      <style dangerouslySetInnerHTML={{ __html: V0_THEME }} />
      {view === 'merchant' ? (
        <MerchantDashboard />
      ) : view === 'admin' ? (
        <AdminDashboard />
      ) : view === 'client' ? (
        <ClientDashboard />
      ) : (
        <ClientHome go={setView} />
      )}
    </div>
  )
}

